/// Web OAuth client ID from Firebase (`oauth_client` type 3 in google-services.json).
///
/// Passed as `serverClientId` so Google Sign-In returns an `idToken` for Firebase Auth.
/// Safe to commit (public client id, not a secret). Update when regenerating Firebase config.
const kGoogleServerClientId =
    '352490409269-64oji16h1femd548tfhofnp21qqoie28.apps.googleusercontent.com';
